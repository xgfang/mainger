% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mainger.R
\name{mainger}
\alias{mainger}
\title{Mahalanobis-Integrated Regression}
\usage{
mainger(
  X_int = NULL,
  Y_int = NULL,
  beta_int = NULL,
  Sigma_int = NULL,
  r_int = NULL,
  Sigma_ref = NULL,
  beta_ext,
  Sigma_ext = NULL,
  sigma2_int = NULL,
  sigma2_ext = NULL,
  n_int = NULL,
  n_ext = NULL,
  tuning = c("auto", "cv", "eaic", "fixed"),
  eta = NULL,
  eta_grid = NULL,
  nfolds = 5,
  base = c("ols", "ridge"),
  lambda = NULL,
  diagnostics = TRUE
)
}
\arguments{
\item{X_int}{Internal design matrix (n x p). If provided, summary statistics
are computed automatically.}

\item{Y_int}{Internal response vector (n x 1).}

\item{beta_int}{Internal point estimate (p-vector). Computed from X_int, Y_int
if not provided.}

\item{Sigma_int}{Internal Gram matrix X'X/n (p x p).}

\item{r_int}{Marginal correlations X'Y/n (p-vector). For restricted scenario.}

\item{Sigma_ref}{Reference LD/covariance matrix (p x p). For restricted scenario.}

\item{beta_ext}{External point estimate (p-vector). Required.}

\item{Sigma_ext}{External Gram matrix (p x p). If provided, uses full scenario.}

\item{sigma2_int}{Internal error variance. Estimated if X_int, Y_int provided.}

\item{sigma2_ext}{External error variance (for full scenario bounds).}

\item{n_int}{Internal sample size.}

\item{n_ext}{External sample size.}

\item{tuning}{Tuning method: "auto" (default), "cv", "eaic", or "fixed".}

\item{eta}{Fixed eta value (when tuning = "fixed").}

\item{eta_grid}{Custom grid for eta search. Default: bounded by theory.}

\item{nfolds}{Number of CV folds (default 5).}

\item{base}{Internal estimator type: "ols" (default) or "ridge".}

\item{lambda}{Ridge penalty. NULL = auto via cv.glmnet.}

\item{diagnostics}{Logical. Compute spectral diagnostics? (default TRUE)}
}
\value{
An object of class "mainger" containing coefficients, tuning results,
and diagnostics.
}
\description{
Unified interface for integrating external information into an
internal regression model. Automatically detects the data-sharing scenario
from the provided inputs and selects the appropriate estimator and tuning
strategy.
}
\examples{
\dontrun{
# Partial sharing with individual data
fit <- mainger(X_int = X, Y_int = Y, beta_ext = theta_hat)
summary(fit)
plot(fit)

# Restricted sharing with marginal correlations
fit_r <- mainger(r_int = XtY_over_n, Sigma_ref = LD_matrix,
                 beta_ext = gwas_betas, n_int = 2000)
}

}
