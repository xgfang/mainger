% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{spectral_advantage}
\alias{spectral_advantage}
\title{Spectral advantage function A(eta)}
\usage{
spectral_advantage(k_vals, b_vals, sigma2_int, n_int, eta_grid)
}
\arguments{
\item{k_vals}{Vector of eigenvalues k_j (from \code{spectral_condition_ratio})}

\item{b_vals}{Vector of direction-specific external risks b_j (same length as k_vals)}

\item{sigma2_int}{Internal error variance}

\item{n_int}{Internal sample size}

\item{eta_grid}{Vector of eta values at which to evaluate A(eta)}
}
\value{
A data.frame with columns eta, mse_partial, mse_full, advantage
}
\description{
Computes A(eta) = MSE_partial(eta) - MSE_full(eta) using the per-direction
decomposition from Proposition 2 (corrected). When A > 0, full sharing
outperforms partial; when A < 0, partial outperforms full.
}
\details{
Given estimates of the direction-specific biases b_j (expected squared
difference between theta_j and beta_1j in the diagonalized basis),
this function evaluates the exact MSE gap using
g_j(alpha) = (V + alpha^2 * b_j) / (1 + alpha)^2, where V = sigma2_int / n_int.
}
